[project]
name = "per-scholas-locked-glabs"
version = "1.0.0"
description = "Completed GLAB 927.4.1 and GLAB 927.6.1 in a reproducible uv environment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain-core>=0.3,<2",
    "langchain-groq>=0.2,<2",
    "textblob>=0.18,<1",
]

[tool.uv]
package = false
